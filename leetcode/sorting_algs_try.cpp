#include <cstdio>
#include <iostream>
#include <cstdlib>
#include <vector>

using namespace std;

void selection_sort(vector<int> tosort){
    int ii, jj, pos, minval;
    for (ii = 0; ii<tosort.size(); ii++){
        minval = tosort[ii];
        pos = ii;
        for (jj = ii+1; jj<tosort.size(); jj++){
            if tosort[jj]
    }
}

int main(void){
    vector<int> tosort({1,7,4,8,0,3,4,6,1,7,3,2,1});
    vector<int> tosort1(tosort);
    vector<int> tosort2(tosort);
    vector<int> tosort3(tosort);
    return 0;
}